const fs=require('node:fs');
const path=require('node:path');
const {milestones,tests}=require('./foundation-plan-data.cjs');
const root=path.resolve(__dirname,'..');
const check=process.argv.includes('--check');
const sourcePath='packages/soccer_agent_activity_package_20260908/docs/soccer_linear_manifest.json';
const source=JSON.parse(fs.readFileSync(path.join(root,sourcePath),'utf8'));
const bySource=new Map(source.tasks.map(t=>[t.id,t]));
const date='2026-09-10';
const link=(id)=>`[${id}](../delivery/soccer_delivery_backlog.md)`;
const fixtureDefinitions={
  'FX-PROTOCOL':'Draft measurement protocol and proposed device/threshold list; reviewer acceptance and date stay empty until the actual review occurs.',
  'FX-ENV':'Clean disposable checkout/runner; pinned tool versions; no production connection; synthetic secret canaries only.',
  'FX-PLAN':'Current source/activity manifests and acceptance register; foundation planning records; no actual completion data invented.',
  'FX-CONTRACT':'Versioned valid/invalid payloads with expected errors, ownership boundaries and clock/event identity; no guessed coaching rules.',
  'FX-IDENTITY':'Two unrelated households, two clubs, guardian/coach/player/alternate actors, multi-role and revoked/expired states; real ephemeral test sessions where authorization is evaluated.',
  'FX-CLOCK':'Injected monotonic clock, deterministic event schedule, fake capture start/stop callbacks and lifecycle counters; evidence is unit/widget simulation only.',
  'FX-LOCAL':'Real disposable SQLite databases, per-context file catalogs, migration fixtures, outbox IDs, crash points and server suppression records.',
  'FX-AGENT':'Exact target/base/file boundary, actual named reviewer and harmless trial patch; explicit cancellation and attempt/spend ceilings.',
  'FX-MEDIA':'Licensed logo and adult/synthetic footage with hashes, orientation/audio/chapters; named device/OS/codec; pre-approved thresholds and safe storage/thermal envelope.',
  'FX-BILLING':'Actual authorized provider sandbox accounts and receipts/events, distinct beneficiaries, expected source ledger and safe webhook capture; no live charges.'
};
const cases=tests.map(([n,milestone,title,level,fixture,precondition,actions,expected,sourceTask])=>({id:`DF-T${n}`,milestone:`DF-${milestone}`,title,level,fixture_id:fixture,preconditions:[precondition,fixtureDefinitions[fixture]],steps:actions.split('; '),expected_result:expected,source_task:sourceTask,source_acceptance_ids:bySource.get(sourceTask).acceptance_ids,status:'Specified; not implemented or executed',test_file:null,runner_version:null,last_run_commit:null,evidence:[],reviewer:null,accepted_at:null}));
const milestonesOut=milestones.map(m=>({...m,status:'Proposed; not started',source_tasks:m.source,prepares_source_tasks:m.prepares,source_activity_ids:m.source.flatMap(id=>bySource.get(id).activity_ids),source_acceptance_ids:m.source.flatMap(id=>bySource.get(id).acceptance_ids),work:m.work.map(([title,goal,deliverable,done],i)=>({id:`${m.id}-${String(i+1).padStart(2,'0')}`,title,goal,deliverable,acceptance:done,depends_on:i?[`${m.id}-${String(i).padStart(2,'0')}`]:m.depends.map(id=>`${id}-03`),executor_role:m.owner,reviewer_role:m.reviewer,status:'Proposed; not started',run_contract:{repository_url:'https://github.com/sumarahmed/SoccerAPP',base_commit:null,branch:null,environment:null,approved_files:null,owner_name:null,reviewer_name:null,authority_record:null,approved_minutes:null,approved_spend:null,max_attempts:null},evidence:[],accepted_at:null}))}));
const record={schema_version:'1.0',planning_date:date,scope:'Planning/refinement and GitHub documentation publication only',implementation_authorized:false,source_manifest:sourcePath,source_counts:{tasks:165,activities:390,acceptance_criteria:613},counting_rule:'DF milestone/work/test IDs are planning decompositions mapped to existing source records, not additional imported SP/ACT tasks. Do not add these counts to source throughput.',milestones:milestonesOut};
const testRecord={schema_version:'1.0',planning_date:date,status:'Written test specifications only',fixtures:fixtureDefinitions,cases};
function emit(file,text){const p=path.join(root,'docs/foundation',file);if(check){if(!fs.existsSync(p)||fs.readFileSync(p,'utf8')!==text)throw Error('Stale generated foundation document: '+file);}else{fs.mkdirSync(path.dirname(p),{recursive:true});fs.writeFileSync(p,text);}}
emit('foundation_milestones.json',JSON.stringify(record,null,2)+'\n');
emit('foundation_test_catalog.json',JSON.stringify(testRecord,null,2)+'\n');
const preamble=`# Detailed development foundation milestones\n\nPrepared 10 September 2026. **11 milestones, 33 bounded work items and 36 written foundation test cases.** This plan turns the documented technology stack into a sequence for building a reproducible development environment and proving risky integrations. No SDK, library, app scaffold, cloud service or executable app test is installed by this document.\n\nRead the [technology/library baseline](technology_and_libraries.md), [test catalog](foundation_test_catalog.md), [structured milestone records](foundation_milestones.json) and [review report](foundation_review.md). The [existing activity plan](../delivery/soccer_agent_activity_plan.md) and [master plan](../product/soccer_delivery_master_plan.md) remain the product source of truth.\n\n## Baseline and scope\n\nRepository inspected: \`sumarahmed/SoccerAPP\`, \`main\`, source baseline commit \`667f041649d88d02a2b153a8caf565af3a048269\`. It contains planning artifacts and documentation validators, with no app projects, app dependency locks, backend migrations or app CI workflows. On the inspected Windows host Node/npm/Git/GitHub CLI were on PATH; Flutter/Dart/Android device tools/Docker/Supabase CLI were not found on PATH. This does not prove they are absent from every machine or location.\n\nThe foundation is delivered through the existing SP/ACT tasks. **The counts remain 165 SP tasks, 390 ACT activities and 613 planned acceptance criteria.** DF IDs organize this refinement and are not extra Linear imports or completed work. Mapping a source task means contributing evidence to it; it never closes that task automatically. The 8 September package is preserved unchanged. This 10 September plan is an additional authoritative refinement under \`docs/foundation/\`.\n\nBefore executing a work item, satisfy both its DF predecessors and every applicable existing SP predecessor, including real contract, coaching, scope and funding decisions. Planning drafts can proceed within the current request, while implementation stays unstarted. In particular, do not treat scaffolding as completion of SP-090 or SP-085 before SP-018. Early scaffolds and schemas are disposable feasibility harnesses under SP-012/SP-039/SP-051.\n\n## Milestone sequence\n\n| Milestone | Outcome | DF predecessors | Accountable role |\n|---|---|---|---|\n${milestones.map(m=>`| [${m.id}](#${m.id.toLowerCase()}) | ${m.title} | ${m.depends.join(', ')||'None; source decisions still required'} | ${m.owner} |`).join('\n')}\n\nDF-02 (CI) and DF-03 (contracts/fixtures) can proceed in parallel after DF-01 where their source inputs allow it. Other apparent parallel opportunities require non-overlapping file ownership and accepted interfaces; no agents have been launched.\n\n**Checkpoint A — workspace usable:** DF-00 through DF-07 accepted, with actual SDKs, local services, executable local tests, enforcement and one reviewed agent run. This does not prove native camera/export or provider billing.\n\n**Checkpoint B — feasibility measured:** DF-08 and DF-09 add physical-device and genuine provider sandbox evidence. Missing macOS/iPhone access or a sandbox account cannot be substituted with a mock pass.\n\n**Checkpoint C — full-build decision:** DF-10 reviews these results together with every remaining SP-018 predecessor. Content approval, security review and revised funding remain required; this checkpoint is not pilot readiness or release approval.\n\n## Assignment, estimates and evidence\n\nEach work item below states an executor/reviewer role, goal, deliverable, proposed file area and acceptance condition. Resolve actual people, base SHA, environment, allowed files and run/spend ceiling before Ready. The structured records intentionally leave these values blank. Proposed first checkpoint is 90 minutes/two attempts, inherited from the execution playbook, only after an actual ceiling is accepted; larger work must be split by the named behavior/interface. No dates, total budget or completion percentages are promised without actual capacity and device/provider access. Estimate hands-on work separately from account, review and equipment waits; do not sum a milestone and its children.\n\nAn implementing agent may validate its change and submit Review. A named authorized reviewer accepts evidence. Security-sensitive scope uses the designated security reviewer, and actual human gates require actual people. Never satisfy independent review by inventing another approver. One writer owns each actual mutable path/interface; shared fixture changes require coordinating every consuming suite.\n\nEvery evidence record contains DF work/test ID; source SP/ACT/AC IDs; commit/build/config/content/fixture versions; actual command and exit code; tool/OS/device/provider mode; pass/fail/skipped counts; sanitized artifact link/hash; observed versus expected behavior; untested limits; reviewer and decision time. Source criteria remain unverified until this evidence exists.\n\n## Failure and recovery policy\n\n- On a failed install/build/test, keep the failing output and identify the smallest correction; rerun affected cases on the new SHA. A retry pass does not erase the initial failure.\n- A required skipped/zero-test suite or missing device/provider evidence blocks the applicable checkpoint. Distinguish an unavailable service from a product failure.\n- Local destructive reset/seed commands must positively identify a disposable target; preserve useful unsynced data and fixture evidence before reset. Source and exported media are different copies.\n- Missing branch-rule support, reviewer, toolchain, Mac/device, sandbox access or cost authority gets an owner and next action. Do not silently weaken an acceptance criterion.\n- No live charges, customer data, real-child footage, production deployment or unapproved invitation is part of the foundation experiment. Reuse already-recorded authority where it applies.\n\n## Detailed milestone cards\n`;
let cards='';
for(const m of milestonesOut){
  cards+=`\n<a id="${m.id.toLowerCase()}"></a>\n\n### ${m.id} — ${m.title}\n\n**Goal:** ${m.goal}\n\n**Status:** Proposed; not started. **Owner/executor role:** ${m.owner}. **Reviewer:** ${m.reviewer}.\n\n**DF prerequisites:** ${m.depends.join(', ')||'None'}. **Source work:** ${m.source.map(link).join(', ')}. **Prepares but does not complete:** ${m.prepares.map(link).join(', ')||'No additional source tasks'}.\n\n**Entry inputs:**\n\n${m.inputs.map(x=>'- '+x).join('\n')}\n\n**Proposed file boundaries:** ${m.paths.map(x=>'`'+x+'`').join(', ')}. Replace these areas with exact approved files for each run.\n`;
  for(const w of m.work)cards+=`\n#### ${w.id} — ${w.title}\n\n- **Goal:** ${w.goal}\n- **Deliverable:** ${w.deliverable}\n- **Predecessors:** ${w.depends_on.join(', ')||'DF-00 source inputs and actual assignment'}\n- **Completion check:** ${w.acceptance}\n- **Handoff:** actual artifacts/checks and remaining limits to ${m.reviewer}; no self-approval of a human/source gate.\n`;
  cards+=`\n**Milestone exit checks:**\n\n${m.exit.map(x=>'- [ ] '+x).join('\n')}\n\n**Test specifications:** ${m.tests.map(id=>`[${id}](foundation_test_catalog.md#${id.toLowerCase()})`).join(', ')}. These are written expectations, not executed tests. Cross-milestone cases can be rerun by later integration work; original test ownership stays in the catalog.\n`;
}
emit('development_foundation_milestones.md',preamble+cards+`\n## First commissioned implementation batch\n\nStart with DF-00-01 (real owners/authority), DF-00-02 (technical decisions) and DF-00-03 (device/measurement protocol), honoring the recorded work-item sequence. Once their source prerequisites are accepted, DF-01 creates the reproducible harness. DF-02 and DF-03 then provide the first opportunity for separate platform and contract agents. Current authorization covers preparing/reviewing/publishing this plan; this list does not itself start implementation.\n\n## Maintaining this refinement\n\nEdit \`tools/foundation-plan-data.cjs\` for milestone/work/test definitions and regenerate with \`node tools/build-foundation-docs.cjs\`. The library baseline and review report are authored directly. Run \`node tools/validate-docs.cjs\`, including the foundation validator, before committing. An eventual live tracker update must map DF work to actual source issue IDs instead of importing a second competing backlog.\n`);
let testText=`# Foundation test case catalog\n\nPrepared ${date}. **36 specified cases; zero executable test files or run results created by this planning update.** Each case has concrete preconditions, procedure, expected result and existing source acceptance IDs. Implementation milestones must add actual test paths and runners; human/device cases require actual witnessed evidence. See the [milestone plan](development_foundation_milestones.md) and [machine-readable catalog](foundation_test_catalog.json).\n\n## Fixtures and execution boundaries\n\n| Fixture | Contract |\n|---|---|\n${Object.entries(fixtureDefinitions).map(([id,description])=>`| ${id} | ${description} |`).join('\n')}\n\nFixtures have a manifest with version/hash, builder, clock/time-zone, ownership and cleanup policy. Credentials are ephemeral and excluded from artifacts. Reset test state between cases. Include UTC plus Australia/Sydney week/DST boundaries where event ordering or reminders are involved; coaching limits come from accepted contracts. Foundation fixtures do not replace the later complete IP-01–IP-05, accessibility, commerce and release suites under SP-127/SP-165.\n\n## Runner selection\n\nUse Flutter SDK tests for pure/mobile widget behavior, real SQLite for persistence, pgTAP plus HTTP integration for backend policies, Vitest for web units/components and Playwright for browser/server journeys. Physical-media and store sandbox cases are separate lanes. Native permission dialogs need reviewed native automation or a witnessed manual protocol. A fake clock/camera case is explicitly labeled and never counts as a hardware result. See [library and testing rationale](technology_and_libraries.md).\n\n## Case index\n\n| ID | Owner milestone | Case | Evidence level |\n|---|---|---|---|\n${cases.map(c=>`| [${c.id}](#${c.id.toLowerCase()}) | ${c.milestone} | ${c.title} | ${c.level} |`).join('\n')}\n`;
for(const c of cases)testText+=`\n<a id="${c.id.toLowerCase()}"></a>\n\n## ${c.id} — ${c.title}\n\n**Owner milestone:** ${c.milestone}. **Level:** ${c.level}. **Fixture:** ${c.fixture_id}.\n\n**Given:** ${c.preconditions.join(' ')}\n\n**When:**\n\n${c.steps.map((s,i)=>`${i+1}. ${s}`).join('\n')}\n\n**Expected:** ${c.expected_result}.\n\n**Traceability:** ${link(c.source_task)}; ${c.source_acceptance_ids.join(', ')}. Mapping retains the complete source criteria; this one case may establish only part of their evidence.\n\n**Execution status:** ${c.status}. Test file, pinned runner, run commit, evidence and reviewer acceptance are unfilled.\n`;
emit('foundation_test_catalog.md',testText);
console.log(`${check?'Verified':'Generated'} 11 foundation milestones, 33 work items and 36 written test specifications.`);
